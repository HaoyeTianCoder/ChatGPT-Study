The function `unique_day` checks whether a given day occurs only once in a list of possible birthdays. If the day occurs only once, the function returns `True`. If the day occurs more than once, the function returns `False`.