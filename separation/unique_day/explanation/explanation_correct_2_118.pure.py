This function takes in a `day` and a list of `possible_birthdays`, and checks if the `day` appears exactly once as the second element of any tuple in the list. If so, it returns `True`; otherwise, it returns `False`.