The function `unique_month` takes a `month` and a list of `possible_birthdays` as input and returns `True` if the given month appears only once in the list of possible birthdays, otherwise it returns `False`.